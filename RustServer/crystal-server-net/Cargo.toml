[package]
name = "crystal-server-net"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["net", "rt-multi-thread", "macros", "io-util", "time"] }
crystal-shared-proto = { path = "../crystal-shared-proto" }
